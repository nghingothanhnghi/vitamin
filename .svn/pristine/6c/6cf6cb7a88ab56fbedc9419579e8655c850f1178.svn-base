import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ChangeResidentIdComponent } from './change-resident-id.component';

describe('ChangeResidentIdComponent', () => {
  let component: ChangeResidentIdComponent;
  let fixture: ComponentFixture<ChangeResidentIdComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ChangeResidentIdComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ChangeResidentIdComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
