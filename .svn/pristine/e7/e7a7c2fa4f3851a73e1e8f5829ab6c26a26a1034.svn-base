import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DeliPopupComponent } from './deli-popup.component';

describe('DeliPopupComponent', () => {
  let component: DeliPopupComponent;
  let fixture: ComponentFixture<DeliPopupComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ DeliPopupComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(DeliPopupComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
