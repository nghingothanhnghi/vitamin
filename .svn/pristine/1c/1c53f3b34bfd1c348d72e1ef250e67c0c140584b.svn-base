import { ComponentFixture, TestBed } from '@angular/core/testing';

import { MonthlyOrderCountFilterComponent } from './monthly-order-count-filter.component';

describe('MonthlyOrderCountFilterComponent', () => {
  let component: MonthlyOrderCountFilterComponent;
  let fixture: ComponentFixture<MonthlyOrderCountFilterComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ MonthlyOrderCountFilterComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(MonthlyOrderCountFilterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
