import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LastBroadcastComponent } from './last-broadcast.component';

describe('LastBroadcastComponent', () => {
  let component: LastBroadcastComponent;
  let fixture: ComponentFixture<LastBroadcastComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ LastBroadcastComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(LastBroadcastComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
