import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RegCartComponent } from './reg-cart.component';

describe('RegCartComponent', () => {
  let component: RegCartComponent;
  let fixture: ComponentFixture<RegCartComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ RegCartComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(RegCartComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
