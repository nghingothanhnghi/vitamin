import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ZoomLiveComponent } from './zoom-live.component';

describe('ZoomLiveComponent', () => {
  let component: ZoomLiveComponent;
  let fixture: ComponentFixture<ZoomLiveComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ZoomLiveComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ZoomLiveComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
