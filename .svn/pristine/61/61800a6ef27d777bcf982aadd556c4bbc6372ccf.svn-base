import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DateTimeFilterMobileComponent } from './date-time-filter-mobile.component';

describe('DateTimeFilterMobileComponent', () => {
  let component: DateTimeFilterMobileComponent;
  let fixture: ComponentFixture<DateTimeFilterMobileComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ DateTimeFilterMobileComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(DateTimeFilterMobileComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
